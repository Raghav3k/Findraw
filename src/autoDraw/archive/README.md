# Archived AutoDraw zoom reveal

These files preserve the previous close-up-to-full-sketch reveal mechanic as it existed before the contextual scribble reveal replaced it.

They use the `.archived` suffix intentionally so TypeScript and Vite do not compile unused legacy code. The live implementation is in `../AutoDrawCanvas.tsx` and `../autoDrawAssets.ts`.
