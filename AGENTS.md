# Findraw UI conventions

## Scrollbars

- Use the global Findraw scrollbar styling in `src/styles.css` for every new scrollable surface.
- Do not add browser-default-looking or component-specific scrollbar themes.
- Keep tracks transparent, thumbs slim and rounded, and hover contrast subtle.
- Prefer `overflow-x: hidden` on vertical panels unless horizontal scrolling is an explicit product requirement.